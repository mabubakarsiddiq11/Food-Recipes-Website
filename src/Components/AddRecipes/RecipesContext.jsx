import React, { createContext, useState } from 'react'

const recipeProvide = createContext()

function RecipesContext({children}) {
    const [recipe,setRecipe] = useState()
  return (
    <>
      
    </>
  )
}

export default RecipesContext
